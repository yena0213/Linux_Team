# Shell 기반 타이핑 게임 시스템

**Linux 프로그래밍 실습 과제**

## 📋 프로젝트 정보

- **팀원**: 김준성(202201465), 이예나(202201503)
- **Repository**: https://github.com/yena0213/Linux_Team
- **개발 언어**: Bash Shell Script
- **개발 환경**: macOS, Ubuntu Linux

---

## 🎮 프로젝트 개요

Shell Script를 활용한 CLI 기반 타이핑 게임입니다. 사용자는 난이도를 선택하고, 랜덤으로 출제되는 단어를 입력하여 점수를 획득합니다. 시간, 정확도, 난이도에 따라 점수가 계산되며, 랭킹 시스템을 통해 상위 5명의 기록을 확인할 수 있습니다.

---

## ✨ 주요 기능

1. **타이핑 게임**
   - 난이도 선택 (Easy/Normal/Hard)
   - 랜덤 단어 출제
   - 정확도 및 시간 측정
   - 실시간 피드백

2. **랭킹 시스템**
   - 점수 자동 저장
   - 상위 5명 표시
   - 날짜별 기록 관리

3. **로그 관리**
   - 게임 실행 로그 기록
   - 레벨별 필터링 (INFO/WARN/ERROR/DEBUG)
   - 최근 로그 조회

4. **설정 관리**
   - 환경변수 조회/설정
   - 단어 파일 경로 변경

5. **시스템 정보**
   - CPU, 메모리, 디스크 사용량
   - 실행 중인 프로세스 정보
   - 시스템 로드 평균

6. **게임 통계**
   - 단어 파일 분석 (총 개수, 평균 길이, 가장 긴 단어)
   - 플레이어별 통계 (플레이 횟수, 평균 점수, 최고 기록)

---

## 🚀 실행 방법

### 1. 저장소 클론

```bash
git clone https://github.com/yena0213/Linux_Team.git
cd Linux_Team/src
```

### 2. 실행 권한 부여

```bash
chmod +x main.sh
```

### 3. 게임 실행

```bash
./main.sh
```

---

## 📁 파일 구조

```
Linux_Team/
├── src/
│   ├── main.sh           # 메인 프로그램 및 메뉴 시스템
│   ├── config.sh         # 전역 변수, 공통 함수, 시그널 처리
│   ├── difficulty.sh     # 난이도 선택 기능
│   ├── game.sh           # 타이핑 게임 로직
│   ├── score.sh          # 점수 계산 시스템
│   ├── ranking.sh        # 랭킹 저장/조회 기능
│   ├── log_viewer.sh     # 로그 조회 및 필터링
│   ├── setting.sh        # 설정 메뉴
│   ├── system_info.sh    # 시스템 정보 및 게임 통계
│   └── word.txt          # 단어 파일 (300개 단어)
├── .github/
│   └── ISSUE_TEMPLATE/   # GitHub 이슈 템플릿
├── ranking.txt           # 랭킹 데이터 (자동 생성)
├── game.log              # 게임 로그 (자동 생성)
├── README.md             # 프로젝트 설명
└── Report.md             # 최종 보고서
```

---

## 📖 단어 파일 안내

### 기본 동작

프로그램은 다음 우선순위로 단어 파일을 찾습니다:

1. `src/word.txt` (프로젝트에 포함된 파일)
2. `/usr/share/dict/words` (시스템 기본 사전)
3. 둘 다 없으면 에러 메시지 출력

### word.txt가 없는 경우

**Windows 또는 /usr/share/dict/words가 없는 Linux 배포판**에서는 프로젝트에 포함된 `src/word.txt`를 사용합니다.

Git 클론 시 `src/word.txt`가 자동으로 포함되므로 별도 작업 불필요합니다.

### 커스텀 단어 파일 사용

`src/word.txt`를 수정하거나, 설정 메뉴(4번)에서 다른 파일 경로를 지정할 수 있습니다.

**파일 형식:**
```
hello
world
linux
shell
...
```
(한 줄에 하나의 단어)

---

## 🎯 점수 계산 방식

```
정확도(%) = (맞춘 단어 / 총 단어) × 100
기본 점수 = 정확도 × 난이도 배수
최종 점수 = 기본 점수 - 시간 페널티
```

**난이도 배수:**
- Easy: 1배 (2단어)
- Normal: 2배 (3단어)
- Hard: 3배 (5단어)

**시간 페널티:**
- 걸린 시간(초)만큼 감점
- 최종 점수가 음수가 되지 않도록 0점 보정

---

## 🛠️ 사용된 기술

### Shell 명령어 (30개 이상)

**텍스트 처리**: `awk`, `sed`, `tr`, `xargs`, `grep`
**정렬/필터링**: `sort`, `head`, `tail`, `shuf`, `wc`
**파일 관리**: `cat`, `touch`, `tee`, `read`, `echo`, `printf`
**시스템 정보**: `ps`, `whoami`, `hostname`, `uname`, `date`, `df`, `uptime`
**제어/환경**: `trap`, `export`, `source`, `clear`, `sleep`, `exit`

### Shell 프로그래밍 기법

- 함수 정의 및 모듈화
- 파이프라인 (5개 이상 명령어 연결)
- 명령 치환 `$()`
- 산술 연산 `$(())`
- 조건문 (`if`, `case`)
- 반복문 (`while`, `for`)
- 시그널 처리 (`trap`)
- 환경변수 관리
- 파일 입출력 및 리다이렉션

---

## 📊 Git 관리

### 브랜치 전략

- `main`: 최종 결과물 (직접 commit 금지)
- `Feat#*`: 새로운 기능 구현
- `chore#*`: 세팅 및 환경 설정
- `fix#*`: 버그 수정

### 커밋 규칙

```
[태그] 작업 내용
```

**예시:**
- `[game] 라운드 진행 구조 구현`
- `[score] 점수 계산 함수 완성`
- `[ranking] 정렬 출력 추가`

### 통계

- 총 커밋: 29개
- Pull Request: 6개
- Feature Branch: 6개
- main 직접 commit: 0회 

---

## 🐛 문제 해결

### 실행 권한 에러

```bash
chmod +x main.sh
```

### word.txt가 없는 경우

Git에서 클론하면 `src/word.txt`가 자동으로 포함됩니다.
만약 없다면:

```bash
cd src
# 시스템 사전 복사 (macOS/Linux)
cp /usr/share/dict/words word.txt

# 또는 간단한 단어 파일 생성
cat > word.txt << 'EOF'
hello
world
linux
shell
EOF
```

### /usr/share/dict/words가 없는 경우

프로젝트에 포함된 `src/word.txt`를 사용하므로 문제없습니다.

